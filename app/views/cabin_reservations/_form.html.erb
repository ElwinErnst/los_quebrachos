<%= form_with(model: cabin_reservation) do |form| %>
  <% if cabin_reservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cabin_reservation.errors.count, "error") %> prohibited this cabin_reservation from being saved:</h2>

      <ul>
        <% cabin_reservation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reservation_id %>
    <%= form.text_field :reservation_id %>
  </div>

  <div class="field">
    <%= form.label :cabin_id %>
    <%= form.text_field :cabin_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
